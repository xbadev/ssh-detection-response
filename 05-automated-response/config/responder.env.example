# Phase 5 responder env template (SAFE TO COMMIT)
# Real version copied to: /etc/ssh-bruteforce-responder.env
# The real WEBHOOK URL is not commited

# Discord webhook URL (preferred variable name used by the script)
ALERT_WEBHOOK_URL="https://example.com/webhook"

# Optional fallback variable name (script also accepts this)
# WEBHOOK_URL="https://example.com/webhook"

